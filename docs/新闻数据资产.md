# A股新闻数据资产（NEWS DATA）

> 更新时间：2026-08-18
> 定位：**新闻/舆情类数据资产**——采集、存储、提炼为新闻因子的管线；⚠️ 2026-08-18 已落地"公告情绪因子"轻量版（实时接口，不走本表完整管线的采集/存库）。
> 状态：✅ **已在选股落地公告情绪因子**（`data/news_sentiment.py`，实时拉个股公告+关键词分类）；完整日频管线（采集→存库→L1-L4）仍 🟡 部分规划未落地（见待办）
> 关联：股票列表 `code_name_map.csv`（代码→公司名映射）是新闻→股票关联的桥梁；因子接入走 [L1_单因子精炼.md](./四层loop架构细则/L1_单因子精炼.md)（信息轴 A6）

---

## 一、目标

现有 45 因子全来自价/量/换手（纯量价维度）。新闻是**全新的信息维度**：市场情绪、事件驱动、热度变化——这些在 K 线里看不到。新闻因子管线把财经新闻转化为日频因子，经 L1-L4 标准流程验证后进 v7 打分。

## 二、数据源规划（待选）

| 数据源 | 特点 | 适用因子类型 | 建议 |
|--------|------|-------------|------|
| 财联社电报（cls.cn） | 事件驱动最强、快讯密度高、A股短线最相关 | 事件因子、情绪因子 | ⭐ 首选 |
| 东方财富快讯/7x24 | 量大、覆盖面广 | 热度因子 | 备选 |
| 新浪财经/同花顺 | 一般 | — | 备选 |
| 东财股吧 | 散户情绪真实但噪音大 | 情绪因子（需强清洗） | 谨慎 |

**历史深度问题**：新闻因子需要历史数据才有回测意义。若只有"从现在开始采"，需等数月才能过 L3 回测——**落地前必须先确认历史数据可得性**（数据源 API 是否支持按历史日期拉取）。

## 三、处理管线（设计）

```
采集 → 清洗 → 关联 → 提炼 → 聚合为日频因子 → 接入 L1
```

1. **采集**：定时拉取（盘中每 5-10 分钟 / 收盘全量），落盘原始 JSON
2. **清洗**：去重（同源重复）、过滤广告/行情播报噪音、正文提取
3. **关联**：新闻 → 股票（名称/代码实体识别，用 `stocks_em.parquet` 名称映射）
4. **提炼**：情绪打分（正面/负面/中性 + 强度）+ 主题分类（公告/业绩/重组/监管/行业/宏观）——LLM 逐条打分（准但贵）vs 规则词典（便宜但糙）vs 混合
5. **聚合**：每日每股票 → 新闻情绪因子 / 热度因子 / 事件因子
6. **接入 L1**：作为候选因子走多周期 IC 体检（fwd_1d/5d/10d/20d）

## 四、存储设计（待建）

```
D:\quant_data\news\
├── raw\              # 原始采集 JSON（按日期分片，如 2026-08-16.jsonl）
├── parsed\           # 清洗+关联后的结构化数据（parquet：news_id, date, time, stock_code, sentiment, topic, intensity, source）
├── daily_factors\    # 聚合后的日频新闻因子（parquet：date, stock_code, 各因子值）
└── pit_snapshots\    # PIT 快照（与宪法 pit_snapshots 联动）
```

**PIT 红线（宪法强制）**：新闻必须按**发布时间**对齐交易日——T 日 15:00 前发布的新闻才能用于 T 日信号，晚于 15:00 算 T+1。禁止用当日盘后新闻回填当日因子（前视偏差）。

---

## 四B、已落地：公告情绪因子（2026-08-18，轻量实时版）

> 与上方完整管线不同，本版不做采集/存库/日频聚合，而是**选股时实时拉取个股公告 + 关键词情绪 + 直接叠加打分**。是新闻情绪的**第一版落地**，后续可演进为完整管线。

**实现**：
- `code/data/news_sentiment.py`：东财 F10 公告接口 `np-anotice-stock.eastmoney.com` 实时拉个股近期公告标题
- **关键词规则分类**：利好词（业绩预增/中标/回购/增持/合同/订单/扭亏）/ 利空词（亏损/减持/诉讼/处罚/立案/质押/退市/问询/监管函）
- 情绪分 = (利好条数 - 利空条数) / max(总条数, 1)，归一化 -1~+1
- **集成 `daily_picks`**：作为第 6 打分维度，对 Top5 拉公告，情绪分叠加评分（±10 分），展示"🔴利空 / 中性 / 🟢利好"

**验证（2026-08-18）**：
- 奥精医疗（监管工作函）🟢→🔴情绪-1 降权 42.8→32.8
- 艾迪药业 🟢利好 +0.33 加权 42.0→45.4

**当前边界（如实标注）**：
- 数据源仅**公告**（非新闻/舆情资讯、非研报）
- 分类用**关键词规则**（非 LLM，便宜快但糙，可能误判中性公告）
- 实时拉取，**不落库**、不构成日频因子、**未走 L1-L4 完整验证**——只作为选股加分参考，非入池因子
- 情绪分权重（±10 分）为**初始值**，随追踪命中率可调



## 五、新闻因子候选（方向）

| 因子 | 定义 | 逻辑 |
|------|------|------|
| 情绪分 | 当日该股新闻情绪加权均值（-1~+1） | 正面情绪 → 短期动量/反转待验证 |
| 热度变化 | 当日新闻条数 / 5日均值 | 热度突增 = 事件驱动信号 |
| 事件冲击 | 突发利空/利好（强度×方向） | 公告/监管类事件 |
| 板块情绪 | 所属板块当日情绪均值 | 板块联动 |

> 具体因子经 L1 体检（|ICIR|≥0.25 + 同号段≥60% + 衰减<50%）后才有资格进池，规则与量价因子完全一致（见 四层loop架构细则/L1）。

## 六、接入方式

- **数据采集**：新脚本 `news_collector.py`（定时，cron 驱动）
- **提炼**：`news_factor_engine.py`（LLM 打分 + 规则兜底混合）
- **因子落地**：聚合为日频 parquet → 与 ic_data 结构对齐 → 走 `factor_mining_loop.py` 标准 L1-L4 流程
- **半衰期预期**：新闻因子大概率 short_lived（半衰期 <6 月），L4 评估周期自动缩短——符合宪法对短寿命因子的既定处理

## 七、待办清单

- [x] ✅ **公告情绪因子**（2026-08-18 已落地 `data/news_sentiment.py` + 集成 daily_picks，见四B）
- [ ] 数据源扩到**新闻/资讯/舆情**（不只公告）——东财资讯/财联社舆情
- [ ] 实现 news_collector.py（采集+落盘，做日频因子）
- [ ] 实现新闻→股票关联（实体识别，用 `code_name_map.csv`）
- [ ] 实现 news_factor_engine.py（情绪/热度/事件提炼，可用 LLM 提精度）
- [ ] 聚合日频因子，接入 ic_data 结构
- [ ] 用现有量价因子的同一套 L1-L4 流程验证（当前实时版未走此流程）
